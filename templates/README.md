# Templates

The Go SDK keeps go-swagger generation templates in this repository.

This Rust migration keeps the same template files under `templates/` for
repository-shape parity and as reference artifacts alongside the handwritten
Rust implementation in `src/`.
